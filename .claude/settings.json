{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); printf '%s  |  %s' \"$(echo \"$input\" | jq -r '.workspace.current_dir')\" \"$(echo \"$input\" | jq -r '.model.display_name')\""
  }
}
